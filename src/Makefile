all:
	@echo NTOS is currently configured to use 'GNUmakefile'.  Edit 'build.NTOS'
	@echo if this is not what you want.

.DEFAULT:
	@echo NTOS is currently configured to use 'GNUmakefile'.  Edit 'build.NTOS'
	@echo if this is not what you want.

nothing:

which_makefile:
	echo MakeIsMake
